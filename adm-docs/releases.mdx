---
title: Releases
description: Documentation on all aspects of Moodle site administration.
---

import MoodleReleases from "@site/src/components/MoodleReleases/MoodleReleases";
import MoodleReleasesFiltered from "@site/src/components/MoodleReleases/MoodleReleasesFiltered";

## Filtered releases

### Test

- Active LTS branches only

<MoodleReleasesFiltered filterStatus={["lts", "future"]} />

- LTS branches in security-only maintenance

<MoodleReleasesFiltered filterStatus={["lts", "security"]} />

- Still works as before

<MoodleReleasesFiltered filterStatus="active" />

- All LTS branches except EOL ones

  <MoodleReleasesFiltered filterStatus={["lts", "!eol"]} />

- Everything except EOL

  <MoodleReleasesFiltered filterStatus="!eol" />

- Everything except EOL and experimental
  <MoodleReleasesFiltered filterStatus={["!experimental", "!eol"]} />

### Only active branches

```md
<MoodleReleasesFiltered filterStatus="active" />
```

<MoodleReleasesFiltered filterStatus="active" />

{/* Only LTS branches */}

### Only LTS branches

```md
<MoodleReleasesFiltered filterStatus="lts" />
```

<MoodleReleasesFiltered filterStatus="lts" />

### All 5.x branches

```md
<MoodleReleasesFiltered
  filterSeries="5"
  sortCol="releaseDate"
/>
```

<MoodleReleasesFiltered filterSeries="5" sortCol="releaseDate" />

{/* All 4.x branches, oldest first, no patch releases column */}

### All 4.x branches, oldest first, no patch releases column

```md
<MoodleReleasesFiltered
  filterSeries="4"
  sortCol="releaseDate"
  sortDir={1}
  hidePatchReleases
/>
```

<MoodleReleasesFiltered
  filterSeries="4"
  sortCol="releaseDate"
  sortDir={1}
  hidePatchReleases
/>

{/* Upcoming releases */}

### Upcoming releases

```md
<MoodleReleasesFiltered filterStatus="future" />
```

<MoodleReleasesFiltered filterStatus="future" />

{/* Security-only, sorted by security end date */}

### Security-only, sorted by security end date

```md
<MoodleReleasesFiltered filterStatus="security" sortCol="securityEndDate" />
```

<MoodleReleasesFiltered filterStatus="security" sortCol="securityEndDate" />

## All releases

```md
<MoodleReleases />
```

<MoodleReleases />
